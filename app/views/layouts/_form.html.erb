<div class="conteinercheckm">
	<div class="conteinercheckl">
		<div class="conteinercheckr">
			<%= form_for @user_site, :url => {:controller=>"user_sites", :action => "create" } do |f| %>
				<%= errors_for(@user_site).html_safe %>
				<div class="text1">
					<%= t("layout.i_know_good_site") %>:
				</div>

				<div class="intext">
					 <%= f.text_field :url %>
				</div>
				<div class="text1">
					<%= t("layout.it is about") %>:
				</div>
				
				<!-- <select class="selectBlock" style="width:173px;margin:0 0" >	 -->
				<% categories_array = @categories.all.map { |c| [c.name, c.id] } %>							
				<%= f.select (:category_id, categories_array, {:class => "selectBlock", :style =>"width:173px;margin:0 0"}) %>
				
				<%= f.submit "Everyone should watch it", :class =>"buttonadd" %>
			<% end %>
		</div>
	</div>
</div>